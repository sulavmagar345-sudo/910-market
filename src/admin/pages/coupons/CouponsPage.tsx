import { Ticket } from 'lucide-react';
import { ComingSoon } from '../../components/ui/ComingSoon';

export default function CouponsPage() {
  return (
    <ComingSoon
      title="Discount Coupons"
      description="Create promotional codes, set minimum purchase requirements, and track coupon usage. This feature is currently in development."
      icon={Ticket}
    />
  );
}
