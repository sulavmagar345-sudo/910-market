import { Layers } from 'lucide-react';
import { ComingSoon } from '../../components/ui/ComingSoon';

export default function CategoriesPage() {
  return (
    <ComingSoon
      title="Category Management"
      description="Organize your alcohol catalog with multi-level categories, custom sorting, and collection pages. This feature is currently in development."
      icon={Layers}
    />
  );
}
