import { Award } from 'lucide-react';
import { ComingSoon } from '../../components/ui/ComingSoon';

export default function BrandsPage() {
  return (
    <ComingSoon
      title="Brand Directory"
      description="Manage brand pages, logos, manufacturer details, and brand-specific promotions. This feature is currently in development."
      icon={Award}
    />
  );
}
